-- 商品分類ごとの商品数を表示する
SELECT 
	ic.id AS 商品分類コード,
	ic.name AS 分類名,
	COUNT(i.id) AS 商品数
FROM 
	item_class AS ic
LEFT JOIN 
	item AS i
ON 
	ic.id = i.item_class_id
GROUP BY 
	ic.name
ORDER BY
	ic.id ASC
;


※mysqlで教本のテーブル内容に則って作成したデータベースを使用しています。